/**
 * This is the default rank that is used, if no rank is configured.
 */
export const DEFAULT_RANK = "§bMember";

/**
 * The Default Start String used, if no rank is configured.
 */
export const START_STRING = "§r§l§8[§r";

/**
 * The Default Join String used, if no rank is configured.
 */
export const JOIN_STRING = "§r§l§8][§r";

/**
 * The Default End String used, if no rank is configured.
 */
export const END_STRING = "§r§l§8]§r§7";
